name: Backlog Notify
on:
  push:
  pull_request:
    types: [opened, synchronize, closed]

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Backlog Notify
        uses: bicstone/backlog-notify@master
        with:
          api_host: "yhddigital.backlog.com"
          api_key: ${{ secrets.BACKLOG_API_KEY }}
          project_key: "AI_DRIVEN_DEV_PJ"
